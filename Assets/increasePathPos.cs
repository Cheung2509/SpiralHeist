using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using Cinemachine;

public class increasePathPos : MonoBehaviour {

    CinemachineVirtualCamera camera_controller;

	// Use this for initialization
	void Start () {
        camera_controller = GetComponent<CinemachineVirtualCamera>();

    }
	
	// Update is called once per frame
	void Update () {
        camera_controller.

    }
}
